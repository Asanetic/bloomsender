/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/bm',      // dairy sacco graders portal tells Next.js this app lives under /dairy
    assetPrefix: '/bm',   // optional: ensures static assets load correctly
    reactStrictMode: true
  };
  
  module.exports = nextConfig;
  
  